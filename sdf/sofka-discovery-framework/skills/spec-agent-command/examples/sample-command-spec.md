# Sample Command Specification Output

Example of a canonical command .md and its alias, generated by `spec-agent-command`.

---

## Canonical: validate.md

```markdown
---
description: "Run structural validation on a plugin directory."
user-invocable: true
argument-hint: "<plugin-path>"
---

# Validate

## Usage

/pqa:validate <plugin-path>

## Execution Flow

Invokes the `plugin-validator` agent which runs these skills in sequence:

validate-structure -> validate-manifest -> validate-components -> validate-hooks -> validate-cross-refs

## Output

Prints a summary table of findings to the console, classified by severity (CRITICAL, WARNING, INFO). Reports pass/fail status.
```

---

## Alias: v.md

```markdown
---
alias-of: validate
description: "Run structural validation on a plugin directory."
user-invocable: true
---

Alias for `validate`. See `commands/validate.md`.
```
