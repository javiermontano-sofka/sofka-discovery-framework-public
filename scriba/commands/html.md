---
description: "Alias → export-html. Use /scriba:export-html instead."
user-invocable: true
---

# Redirect: html → export-html

This is an alias. The canonical command is `export-html`.

Activate `export-html` skill with all arguments passed through: $ARGUMENTS
