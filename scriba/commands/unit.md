---
description: "Alias → generate-unit. Use /scriba:generate-unit instead."
user-invocable: true
---

# Redirect: unit → generate-unit

This is an alias. The canonical command is `generate-unit`.

Activate `generate-unit` skill with all arguments passed through: $ARGUMENTS
