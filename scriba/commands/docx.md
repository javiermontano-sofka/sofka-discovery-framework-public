---
description: "Alias → export-docx. Use /scriba:export-docx instead."
user-invocable: true
---

# Redirect: docx → export-docx

This is an alias. The canonical command is `export-docx`.

Activate `export-docx` skill with all arguments passed through: $ARGUMENTS
