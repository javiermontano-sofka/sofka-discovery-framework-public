---
description: "Alias → validate-content. Use /scriba:validate-content instead."
user-invocable: true
---

# Redirect: validate → validate-content

This is an alias. The canonical command is `validate-content`.

Activate `validate-content` skill with all arguments passed through: $ARGUMENTS
