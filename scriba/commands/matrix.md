---
description: "Alias → generate-matrix. Use /scriba:generate-matrix instead."
user-invocable: true
---

# Redirect: matrix → generate-matrix

This is an alias. The canonical command is `generate-matrix`.

Activate `generate-matrix` skill with all arguments passed through: $ARGUMENTS
