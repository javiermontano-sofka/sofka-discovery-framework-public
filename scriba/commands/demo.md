---
description: "Alias → run-demo. Use /scriba:run-demo instead."
user-invocable: true
---

# Redirect: demo → run-demo

This is an alias. The canonical command is `run-demo`.

Activate `run-demo` skill with all arguments passed through: $ARGUMENTS
