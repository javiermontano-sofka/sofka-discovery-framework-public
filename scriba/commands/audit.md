---
description: "Alias → audit-pipeline. Use /scriba:audit-pipeline instead."
user-invocable: true
---

# Redirect: audit → audit-pipeline

This is an alias. The canonical command is `audit-pipeline`.

Activate `audit-pipeline` skill with all arguments passed through: $ARGUMENTS
