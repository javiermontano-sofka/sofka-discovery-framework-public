---
description: "Alias → init-project. Use /scriba:init-project instead."
user-invocable: true
---

# Redirect: init → init-project

This is an alias. The canonical command is `init-project`.

Activate `init-project` skill with all arguments passed through: $ARGUMENTS
